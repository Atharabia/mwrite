# Mwrite

A simple blog app

## Installation

1. Install Docker
2. Clone this repo
3. Copy `.env.example` to `.env` and fill in the values
4. Run the following command:

```bash
docker compose up --build -d
```

5. The app will be available at [http://localhost](http://localhost)
